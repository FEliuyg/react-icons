import React from "react";
import styled from "styled-components";

const Header = styled.header`
  display: block;
`;

export default () => (
  <Header>
    <h1>@easyv/react-icons</h1>
  </Header>
);
